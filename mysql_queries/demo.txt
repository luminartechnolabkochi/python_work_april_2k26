
1)query for listing all databases

    mysql> show databases;

2)query for creating a new database

    mysql> create database database_name;

    mysql>create database ecom_db;

3)query for switching to a databse;

    mysql> use database_name;

    mysql>use ecom_db;



4)query for creating table

    mysql> create table table_name (

        field_name datatype constraint,
        field_name datatype constraint,
        field_name datatype constraint,
        field_name datatype constraint,
    );

[Product => id,title,price,category,description]

    mysql> create table product (

        id int auto_increment primary key,
        title varchar(200) not null,
        price decimal(10,2) not null,
        category enum("electronic","clothing") default "clothing",
        description varchar(200) null


    );


5)query for inserting records into table


    mysql> insert into table_name (column1,column2,,,) values (val1,val2,,)

    mysql> insert into product (title,price,category,description) values('lp shirt',2300,'clothing','lp striped shirt');


